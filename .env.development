# Development Environment Variables
# These are used for local development and dev branch deployments

# Development Database (Same as production for now - will separate later)
# For now, using same database but with different environment flags
DATABASE_URL="postgresql://neondb_owner:npg_qQtsRd68lyrU@ep-lingering-field-ae55l14x-pooler.c-2.us-east-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# NextAuth Configuration for Development
NEXTAUTH_SECRET="dev-secret-key-for-development-only-not-production"
NEXTAUTH_URL="http://localhost:3000"

# SensorPush API Credentials (Same as production for now)
SENSORPUSH_EMAIL="admin@stgeorgesrx.com"
SENSORPUSH_PASSWORD="Pharmacy@07036!"

# Email Configuration for Development (Mock mode)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-dev-email@gmail.com
# SMTP_PASS=your-dev-app-password
# SMTP_FROM=TempRx360 Dev <your-dev-email@gmail.com>

# Development flags
NODE_ENV=development
VERCEL_ENV=development